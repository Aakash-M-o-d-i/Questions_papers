// // Optional JavaScript for controlling the speed of the animation
// const marqueeText = document.getElementById("marquee");
// let speed = 50; // adjust the speed by changing this value

// function adjustMarqueeSpeed() {
//   const marqueeWidth = marqueeText.offsetWidth;
//   const animationDuration = marqueeWidth / speed;
//   marqueeText.style.animationDuration = `${animationDuration}s`;
// }

// window.onload = adjustMarqueeSpeed;
// window.onresize = adjustMarqueeSpeed;
